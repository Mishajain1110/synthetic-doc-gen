# synthetic-doc-gen
Synthetic document generation pipeline for large-scale training datasets using structured document templates (e.g., passports, visas). The system leverages OCR, image and signature detection to generate realistic synthetic document data.
